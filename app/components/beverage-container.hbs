<h1>{{yield}}</h1>
{{#unless @isFav}}
    <button type="button">Show 10 beverages</button>
{{/unless}}
<div class="flex-page">
  <BeverageList @model={{@model}} @checkBevId={{this.showBeverageDetails}}/>
  <BeverageDetails @isFavourite={{@isFav}}/>
</div>